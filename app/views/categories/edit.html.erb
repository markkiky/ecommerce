<%= form_with(model: @category, remote: true ) do |form| %>

    <%= form.label :category_name %><br />
    <%= form.text_field :category_name %>

    <%= form.submit "Update Category" %> | <%= link_to 'Back', categories_path %>
<% end %>

